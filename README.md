
  # Generate HTML Code

  This is a code bundle for Generate HTML Code. The original project is available at https://www.figma.com/design/h2r8zhwGH2fWYmRSUs8s4F/Generate-HTML-Code.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  